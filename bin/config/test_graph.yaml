cost_type: 1
save_file: ${graph.name}_graph_variable_cost_${cost_type}.pdf

defaults:
  - _self_
  - graph: complete



hydra:
  run:
    dir: ${oc.env:XP_PATH}/${hydra.job.name}/${graph.name}
  sweep:
    dir: ${oc.env:XP_PATH}/${hydra.job.name}
    subdir: ${graph.name}
  job:
    config:
      override_dirname:
        kv_sep: _
        item_sep: /